node_modules
dist
.npmcache
.wrangler
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# 本机 Vite 补丁的原始文件备份（见开发记录），不需入库
.patches

# 开发过程文档：需求原文与实施记录，不对外发布
draft.md
plan.md

# ── Go 后端 ──────────────────────────────────────────────────────
# 真实配置（server.example.yaml 是模板，要入库）
server/server.yaml
server/*.exe
# Go 构建缓存与模块缓存（本机 D:\nodejs、%TEMP% 不可写，只能落在工作区）
.gocache/
.gomodcache/
.gotmp/
# 注意：server/vendor/ 是**故意入库**的。
# 后端的唯一外部依赖是 gopkg.in/yaml.v3，vendor 进来只有 327 KB，
# 换来的是「克隆下来就能离线构建」——不必依赖 proxy.golang.org
# （该域名在国内常不可达）。

# 无头浏览器截图与 DOM dump（本地视觉验证用）
.shots/
